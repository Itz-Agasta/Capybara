[package]
name = "pulsar-core"
version = "0.1.0"
edition = "2024"
description = "Pure types and coordinate math — no I/O"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
